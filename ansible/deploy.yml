- name: Deploy Student Feedback App
  hosts: localhost
  tasks:

    - name: Stop existing container
      command: docker stop feedback
      ignore_errors: yes

    - name: Remove existing container
      command: docker rm feedback
      ignore_errors: yes

    - name: Run new container
      command: docker run -d -p 5000:5000 --name feedback student-feedback-app